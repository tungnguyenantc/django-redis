sudo apt update
sudo apt -yV upgrade
sudo groupadd docker
sudo usermod -aG docker $USER
source venv/bin/activate
python manage.py shell
celery -A django_redis worker --loglevel=INFO
python -m venv venv
python3 -m venv venv
source venv/bin/activate
pip install django
pip install django-gmailapi-backend celery[redis]
pip freeze > requirements.txt
django-admin startproject django_redis .
python manage.py migrate
python manage.py runserver
python manage.py startapp celeryapp
python manage.py runserver
source venv/bin/activate
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
nano hello_world.sh
which bash
chmod +x hello_world.sh
./hello_world.sh
/hello_world.sh
./hello_world.sh
source venv/bin/activate
pip install gunicorn
pip freeze > requirements.txt
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
source /home/djangoredis/venv/bin/activate
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
clear
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
source /home/djangoredis/venv/bin/activate
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
clear
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
clear
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
docker build -t d-celery .
docker run --name django-celery -p 80:80 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
docker build -t d-celery .
docker run --name django-celery -p 8000:8000 d-celery
git --version
sudo adduser git
